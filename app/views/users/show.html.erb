<% provide(:title, @user.name) %>
<div class="container">
<div class="row">
	<div class ="col-xs-2">
	</div>
	<div class = "col-xs-4">
		<section>
			<h2>
				<%= @user.name %>
				<br>
			</h2>
			<h4>
				<%= @user.email %>
				<br>
				<%= @user.phone %>
			</h4>
			<br>
			<%= link_to controller: "users", action: "contract", id: current_user do %>
				<div class="btn btn-default">
					<span class="glyphicon glyphicon-briefcase"></span>&nbspКонтракты
				</div>
			<% end %>
			<br>
			<br>
			

			<%= link_to "Изменить данные профиля", edit_user_path(current_user), class:"btn" %>

		</div>
</div>
</div>