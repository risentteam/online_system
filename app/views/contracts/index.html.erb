<div class="container"
<table id="contracts" class="table">
	<thead>
		<tr>
			<th>Номер</th>
			<th>Компания</th>
			<th>Здания</th>
			<th>Описание</th>
			<th>Комментарий</th>
			<th>Начало</th>
			<th>Конец</th>			
		</tr>
	</thead>    
    <tbody>
		<% for contract in @contracts %>
		<tr>
			<td > <%= contract.name_contract %></td>
	    	<td id = "company"> <%= contract.company %></td>
	    	<td id = "building"> <% contract.buildings[0..-2].each do |building| %> <%=building.arrival_address + ";" %> <% end %> <%=contract.buildings[-1].arrival_address%> </td>
	    	<td id = "description"> <%= contract.description %></td>
	    	<td id = "comment"> <%= contract.comment %></td>
	    	<td id = "begin_time"> <%= contract.begin_time.strftime("%d.%m.%Y") %></td>
	    	<td id = "end_time"> <%= contract.end_time.strftime("%d.%m.%Y") %></td>
		</tr>
		<% end %>
	</tbody>	
</table>


<h2>Импорт контрактов</h2>

<%= form_tag import_contracts_path, multipart: true do %>
  <%= file_field_tag :file, class: "btn btn-file" %>
  <%= submit_tag "Отправить" , class: "btn btn-large btn-primary" %>
<% end %>

Выберите файл с контрактами. Номер контракта должен быть уникальным. Разные адреса внутри одного контракта должны быть разделены ";"
Вы можете изменить любую из ячеек двойным щелчком по ней.
</div>
