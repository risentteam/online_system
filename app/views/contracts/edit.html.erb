<% provide(:title, 'Редактирование контракта') %>
<div class="container">
		<h1>Редактирование контракта</h1>
		<%= form_for @contract, :html => {:class => "form-vertical"} do |f| %>
			<div class="row">
				<div class="form-group">
					<label class="col-md-3 control-label">Договор</label>
					<div class="col-md-5">
						<%= f.text_field :name_contract, {placeholder: "Введите № договора", class: "form-control", required: ""} %>
					</div>
				</div>
			</div>
			<div class="row">
				<div class="form-group">
					<label class="col-md-3 control-label">Компания</label>
					<div class="col-md-5">
						<%= f.text_field :company, {placeholder: "Введите компанию", class: "form-control", required: ""} %>
					</div>
				</div>
			</div>
			<div class="row">
				<div class="form-group">
					<label class="col-md-3 control-label">Комментарий</label>
					<div class="col-md-5">
						<%= f.text_field :comment, {placeholder: "Введите комментарий", class: "form-control"} %>
					</div>
				</div>
			</div>
			<div class="row">
				<div class="form-group">
					<label class="col-md-3 control-label">Описание</label>
					<div class="col-md-5">
						<%= f.text_field :description, {placeholder: "Введите описание", class: "form-control", required: ""} %>
					</div>
				</div>
			</div>

			<div class="row">
				<div class="form-group">
					<label class="col-md-3 control-label">Дата начала</label>
					<div class="col-md-5">
						<%= f.date_field :begin_time, {placeholder: "Введите описание", class: "form-control"} %>
					</div>
				</div>
			</div>

			<div class="row">
				<div class="form-group">
					<label class="col-md-3 control-label">Дата конца</label>
					<div class="col-md-5">
						<%= f.date_field :end_time, {placeholder: "Введите описание", class: "form-control"} %>
					</div>
				</div>
			</div>
			<% if @contract.buildings.count>0 %>
				<% for building in @contract.buildings %>
	 			<div class="row">
					<div class="form-group" id ="building_group">
						<label class="col-md-3 control-label">Объект
							<%= link_to "/delete_building/#{@contract.id}/#{building.id}" do %>
							<span class="glyphicon glyphicon-trash"></span>
							<% end %>
						</label>
						 <div class="col-md-5">

						  <%= select_tag :building, options_from_collection_for_select(@list_buildings, 'id', 'arrival_address', building.id), :prompt => "Выберите из списка", :class => "form-control", required: ""%>

							
						</div>
					</div>
				</div>
				<% end %>
				<% else %>
				<div class="row">
					<div class="form-group" id ="building_group">
						<label class="col-md-3 control-label">Объект</label>
						  <div class="col-md-5">
						  <%= select_tag :building, options_from_collection_for_select(@list_buildings, 'id', 'arrival_address'), :prompt => "Выберите из списка", :class => "form-control", required: ""%>
						</div>
					</div>
				</div>
			<% end %>
				<div class = "row" id = "rowaddbtn">
				<div class = "col-xs-4">
				</div>
				<div class = "col-xs 8">
			<button id="addbtn" type="button" class="btn btn-default btn-xs">
						<span class="glyphicon glyphicon-plus"></span>
			</button>
				</div>
				</div>
			<br>	
		<%= f.submit "Принять", class: "btn btn-large btn-primary", id: "submut" %>
		<%= link_to "Назад к контрактам", {controller: "contracts", action: "index"}, class: "btn"%>		
		<% end %>

</div>
